#include <iostream>
using namespace std;
int main() {
    int a, b, sum;
    cout << "Enter the value of a : ";
    cin >> a;
    cout << "\nEnter the value of b : ";
    cin >> b;
    sum = a + b;
    cout << "Sum of a = " << a << " and b = " << b << " is : " << sum;
}
